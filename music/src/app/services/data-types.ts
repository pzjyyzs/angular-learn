export interface Banner {
  url: string;
  imageUrl: string;
  typeTitle: string;
  titleColor: string;
}

export interface SongSheet {
  name: string;
  id: number;
  playCount: number;
  picUrl: string;
}
